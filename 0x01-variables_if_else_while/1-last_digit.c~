#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - A program
 *Descrition: 'last digit'
 *Return; always 0
 */
int main(void)
{
int n;
int ld;
srand(time(0));
n = rand() - RAND_MAX / 2;
x
return (0);
}
